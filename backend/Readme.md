Application to find the average network speed among the countries
the data is extraced from a csv file(sample), and find the average network speed in countires and within states

To run:
make run

To build:
make build

